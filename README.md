# LongTermWargame
Welcome. More to come. This is where the long term wargame lives

To start, activate the virtual environment, then run:
```
$ python3 server.py
```
Then open index.html in your browser.